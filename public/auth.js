const form = document.querySelector('#auth-form'); const page = document.querySelector('.auth-card'); const error = document.querySelector('#error'); let mode = 'login';
function setMode(next) { mode = next; page.classList.toggle('signup-mode', mode === 'signup'); document.querySelectorAll('[data-mode]').forEach((tab) => tab.classList.toggle('active', tab.dataset.mode === mode)); document.querySelector('#login').placeholder = mode === 'login' ? 'you@example.com' : 'you@example.com'; document.querySelector('#login').required = true; document.querySelector('#password').autocomplete = mode === 'login' ? 'current-password' : 'new-password'; error.textContent = ''; }
document.querySelectorAll('[data-mode]').forEach((button) => button.addEventListener('click', () => setMode(button.dataset.mode)));
form.addEventListener('submit', async (event) => { event.preventDefault(); error.textContent = ''; const body = mode === 'login' ? { login: document.querySelector('#login').value, password: document.querySelector('#password').value } : { email: document.querySelector('#login').value, username: document.querySelector('#username').value, displayName: document.querySelector('#displayName').value, password: document.querySelector('#password').value }; const submit = document.querySelector('#submit'); submit.disabled = true; try { const response = await fetch(`/api/auth/${mode}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) }); const data = await response.json(); if (!response.ok) throw new Error(data.error); const invite = new URLSearchParams(window.location.search).get('invite'); if (invite) await fetch(`/api/invites/${invite}/accept`, { method: 'POST' }); window.location.href = '/'; } catch (err) { error.textContent = err.message; } submit.disabled = false; });
setMode('login');
