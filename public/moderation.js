const list=document.querySelector('#members');async function load(){const response=await fetch('/api/workspace/members');if(response.status===401)return location.href='/auth';const members=await response.json();if(!response.ok){list.textContent=members.error||'Moderation access denied.';return}list.innerHTML='';members.forEach(member=>{const row=document.createElement('div');row.className='mod-row';row.innerHTML=`<strong></strong><select><option>member</option><option>moderator</option><option>admin</option></select><button>Timeout</button>`;row.querySelector('strong').textContent=`${member.display_name} (@${member.username})`;row.querySelector('select').value=member.role;row.querySelector('select').onchange=async event=>{await fetch(`/api/workspace/members/${member.id}/role`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({role:event.target.value})})};row.querySelector('button').onclick=async()=>{await fetch(`/api/workspace/members/${member.id}/timeout`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({minutes:10})});row.querySelector('button').textContent='Timed out';};list.append(row)})}load();
